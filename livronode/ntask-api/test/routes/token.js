describe('Routes: Token', () =>{
    const Users = app.models.users;

    describe('POST /token', () =>{
        beforeEach(done => {

        })

        describe('status 200', () =>{
            it('returns authenticated user token', done =>{

            })
        })

        describe('Status 401', () =>{
            it('Throws error when password is incorrect', done => {

            })
            it('Throws error when email not exixts', done =>{

            })
            it('Throws error when fileds are blanks', done =>{
                
            })
        })
    })
})